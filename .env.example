# Environment Variables
# Copy this file to .env and fill in the values

# Robinhood network (testnet)
NEXT_PUBLIC_ROBINHOOD_NETWORK=testnet

# Private key for deployment (never commit!) (required) [secret]
PRIVATE_KEY=